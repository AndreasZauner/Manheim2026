const ROLE_OPTIONS=['Technische Grabungsleitung','Assistenz technische Grabungsleitung','Teilnehmende'];const PROJECT_START=new Date('2026-07-27T00:00:00');const PROJECT_END=new Date('2026-10-09T00:00:00');const STATUS_ORDER={gesetzt:0,zugesagt:1,'zu_kl\u00e4ren':2,anzufragen:3};const rank=role=>{const text=String(role||'').toLowerCase();if(text.includes('assistenz')||text.includes('assist'))return 1;if(text.includes('technische grabungsleitung')||text.includes('technische leitung')||text.includes('technical_lead')||text.includes('technical lead')||text.includes('grabungsleiter')||text.includes('grabungsleit'))return 0;return 2};const parseDate=value=>{if(!value)return null;const parts=String(value).slice(0,10).split('-');if(parts.length===3)return new Date(`${parts[0]}-${parts[1]}-${parts[2]}T00:00:00`);const de=String(value).match(/(\d{2})\.(\d{2})\.(\d{4})/);return de?new Date(`${de[3]}-${de[2]}-${de[1]}T00:00:00`):null};const fmt=value=>{const date=parseDate(value);return date?date.toLocaleDateString('de-DE',{day:'2-digit',month:'2-digit',year:'numeric'}):'offen'};const esc=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');let activeRoleId=null;let sorting=false;function rowRole(row){return row.querySelector('.personnel-role')?.textContent?.trim()||'Teilnehmende'}function rowStart(row){const text=row.querySelector('.personnel-badge.outline')?.textContent||'';const hit=text.match(/(\d{2})\.(\d{2})\.(\d{4})/);return hit?new Date(`${hit[3]}-${hit[2]}-${hit[1]}T00:00:00`):new Date('9999-12-31T00:00:00')}function sortVisibleRows(){if(sorting)return;const select=document.getElementById('personnelSortBy');const host=document.querySelector('#personnelDeploymentView .personnel-rows');if(!host||select?.value!=='role')return;sorting=true;[...host.querySelectorAll('.personnel-row')].sort((a,b)=>rank(rowRole(a))-rank(rowRole(b))||rowStart(a)-rowStart(b)||a.textContent.localeCompare(b.textContent,'de')).forEach(row=>host.appendChild(row));sorting=false}function normalizeRoleOptions(){const box=document.getElementById('personnelRoleOptions');if(!box||box.dataset.threeLevelRoles==='true')return;const current=[...box.querySelectorAll('.personnel-role-choice.active')][0]?.textContent?.trim();box.dataset.threeLevelRoles='true';box.innerHTML=ROLE_OPTIONS.map(role=>`<button class='personnel-role-choice ${role===current?'active':''}' type='button' data-role='${esc(role)}'>${esc(role)}</button>`).join('')}async function getClient(){const config=window.APP_CONFIG||{};const mod=await import('https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm');return window.getManheimSupabaseClient?.(mod.createClient)||mod.createClient(config.SUPABASE_URL,config.SUPABASE_ANON_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}})}async function saveRole(role){if(!activeRoleId||!role)return;const client=await getClient();const {error}=await client.from('participants').update({public_role:role}).eq('id',activeRoleId);if(error){const node=document.getElementById('personnelRoleError');if(node)node.textContent=`Speichern fehlgeschlagen: ${error.message||error}`;return}const button=document.querySelector(`.personnel-role-edit-btn[data-id='${CSS.escape(activeRoleId)}']`);if(button)button.textContent=role;document.getElementById('personnelRoleBackdrop')?.classList.remove('open');const drawer=document.getElementById('personnelRoleDrawer');drawer?.classList.remove('open');drawer?.setAttribute('aria-hidden','true');sortVisibleRows()}function range(person){const start=parseDate(person.availability_from);const end=parseDate(person.availability_to);const total=PROJECT_END-PROJECT_START;if(!start||!end||end<start)return null;const clippedStart=new Date(Math.max(start,PROJECT_START));const clippedEnd=new Date(Math.min(end,PROJECT_END));const left=Math.min(100,Math.max(0,((clippedStart-PROJECT_START)/total)*100));const width=Math.min(100-left,Math.max(4,((clippedEnd-clippedStart)/total)*100));return{left,width}}function color(status){if(status==='gesetzt')return'green';if(status==='zugesagt')return'blue';if(status==='zu_kl\u00e4ren')return'orange';return'gray'}async function exportPdf(){const client=await getClient();const [participants,privateRows]=await Promise.all([client.from('participants').select('id,full_name,public_role,availability_from,availability_to,status,availability_note,source_note'),client.from('participant_private').select('participant_id,phone,email')]);if(participants.error)throw participants.error;if(privateRows.error)throw privateRows.error;const query=(document.getElementById('personnelSearch')?.value||'').trim().toLowerCase();const status=document.getElementById('personnelStatusFilter')?.value||'all';const sort=document.getElementById('personnelSortBy')?.value||'role';const privateById=new Map((privateRows.data||[]).map(row=>[Number(row.participant_id),row]));let rows=(participants.data||[]).filter(row=>(!query||String(row.full_name||'').toLowerCase().includes(query))&&(status==='all'||row.status===status));rows.sort((a,b)=>{if(sort==='name')return String(a.full_name||'').localeCompare(String(b.full_name||''),'de');if(sort==='status')return(STATUS_ORDER[a.status]??99)-(STATUS_ORDER[b.status]??99)||String(a.full_name||'').localeCompare(String(b.full_name||''),'de');if(sort==='start')return(parseDate(a.availability_from)||new Date('9999-12-31'))-(parseDate(b.availability_from)||new Date('9999-12-31'))||String(a.full_name||'').localeCompare(String(b.full_name||''),'de');return rank(a.public_role)-rank(b.public_role)||(parseDate(a.availability_from)||new Date('9999-12-31'))-(parseDate(b.availability_from)||new Date('9999-12-31'))||String(a.full_name||'').localeCompare(String(b.full_name||''),'de')});const body=rows.map(person=>{const p=privateById.get(Number(person.id))||{};const r=range(person);const timeline=r?`<div class='tl'><div class='bar ${color(person.status)}' style='left:${r.left}%;width:${r.width}%'></div></div><small>${fmt(person.availability_from)} - ${fmt(person.availability_to)}</small>`:'<small>Zeitraum unklar</small>';return`<tr><td><b>${esc(person.full_name||'Ohne Namen')}</b><br>${esc(person.public_role||'Teilnehmende')}</td><td>${esc(p.email||'-')}<br>${esc(p.phone||'-')}</td><td>${esc(person.status||'-')}</td><td>${timeline}</td><td>${esc([person.availability_note,person.source_note].filter(Boolean).join(' - ').slice(0,100))}</td></tr>`}).join('');const win=window.open('','_blank');if(!win){window.alert('PDF-Export konnte nicht geoeffnet werden.');return}win.document.write(`<!doctype html><html lang='de'><head><meta charset='utf-8'><title>Personaleinsatz</title><style>@page{size:A4 landscape;margin:14mm}body{font-family:Arial,sans-serif;color:#0f2740}table{width:100%;border-collapse:collapse;font-size:9.5px}th,td{border:1px solid #d7e1ea;padding:5px;vertical-align:top}th{background:#edf3f8}.tl{position:relative;height:18px;border:1px solid #ccd8e4;border-radius:999px;background:#f7fafc}.bar{position:absolute;top:3px;height:12px;border-radius:999px}.green{background:#2f855a}.blue{background:#2b6cb0}.orange{background:#c98216}.gray{background:#687587}small{color:#52677c}</style></head><body><h1>Personaleinsatz Kerpen-Manheim 2026</h1><p>Sortierung wie in der Webansicht. Export mit Kontaktdaten fuer berechtigte Rollen.</p><table><thead><tr><th>Person / Rolle</th><th>Kontakt</th><th>Status</th><th>Teilnahmezeitraum</th><th>Hinweis</th></tr></thead><tbody>${body||`<tr><td colspan='5'>Keine Personen fuer diese Auswahl.</td></tr>`}</tbody></table><script>window.addEventListener('load',()=>setTimeout(()=>window.print(),150));</script></body></html>`);win.document.close()}document.addEventListener('click',event=>{const roleButton=event.target.closest?.('.personnel-role-edit-btn');if(roleButton)activeRoleId=roleButton.dataset.id;const choice=event.target.closest?.('#personnelRoleOptions .personnel-role-choice');if(choice&&choice.parentElement?.dataset.threeLevelRoles==='true'){event.preventDefault();event.stopImmediatePropagation();saveRole(choice.dataset.role)}const exportButton=event.target.closest?.('#personnelPdfExport');if(exportButton){event.preventDefault();event.stopImmediatePropagation();exportPdf().catch(error=>window.alert(`PDF-Export fehlgeschlagen: ${error.message||error}`))}},true);new MutationObserver(()=>{normalizeRoleOptions();sortVisibleRows()}).observe(document.body,{childList:true,subtree:true});window.setInterval(sortVisibleRows,800);